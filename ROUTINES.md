# ルーティン登録手順（claude.ai の Routines UI から）

`crontab` はサーバにcronを置く場合の定義。常時起動のマシンを用意しないなら、**claude.ai のルーティンとして登録する**のが現実的。

## このセッションから登録できない理由

Claude Code のセッションからルーティンを作ると、**発火したセッションにMCPコネクタ（Notion / Slack / Google Calendar / Google Drive）が引き継がれない。** ツールが無い状態で起動するため、何もできずに終わる。

> this trigger stores no MCP connectors, so the sessions it fires will run without connector tools.

コネクタを持たせるには **claude.ai の Routines UI から作る**必要がある。以下の5本を手で登録すること。

## 共通設定

- **環境**：meeting-automation リポジトリをソースに持つ環境
- **コネクタ**：Notion / Slack / Google Calendar / Google Drive の4つを必ず有効にする
- **セッション**：発火ごとに新規セッション
- **タイムゾーン**：UIがJSTで指定できるならJSTのまま。UTC指定ならカッコ内の式を使う

## 登録する5本

| # | 名前 | スケジュール（JST） | UTC cron |
|---|---|---|---|
| 1 | A-1 取締役週次定例：次回分の準備 | 毎週金 12:00 | `0 3 * * 5` |
| 2 | A-2 ＋ C 経営レイヤー定例：次回分の準備 | 毎週金 12:00 | `0 3 * * 5` |
| 3 | B-1 取締役週次定例：議事録生成 | 毎週月 12:00 | `0 3 * * 1` |
| 4 | B-2 経営レイヤー定例：議事録生成 | 毎週月 17:00 | `0 8 * * 1` |
| 5 | リトライ（両会議まとめて） | 毎週火 08:00 | `0 23 * * 1` |

5番のリトライは、Meetの文字起こしの生成が遅れて月曜の2本が空振りした場合に前日分を拾うためのもの。1本目が成功していればステータスが進んでいるので、二重実行を避けて即終了する。

---

## 1. A-1 取締役週次定例：次回分の準備（金12:00）

```
会議議事録の自動化「A-1（取締役週次定例：次回分の準備）」を実行してください。

リポジトリ meeting-automation がこのセッションにチェックアウトされています。

【手順】
1. prompts/_shared-rules.md を読む。全処理に共通するルールで、個別プロンプトの手順より優先する。
2. config/meetings.json を読む。Notion ID・Slackチャンネル・カレンダー予定名・slack_mode が入っている。
3. prompts/board-weekly-prep.md を読み、書かれているとおりに実行する。

【対象日】
TARGET_DATE = 実行日（JST）。「次回」は、この日から10日先までのカレンダー予定のうち、いちばん近い該当予定を指す。

【厳守】
- Slackは config の slack_mode に従う。
- カレンダーに次回予定がない場合は、Notionに部分的に書き込まず、何も書かずに報告して終了する。
- 書き込んでよいのは、新規作成したページのプロパティと、🟩 トグル内「前回の持ち帰り（自動転記）」の表だけ。それ以外の 🟩 / 🟦 / 🤖 / ⏺️ には触れない。
- 同じ日付のページが既にある場合は作成せず、Slack投稿だけ行う。

最後に、prompts/board-weekly-prep.md 末尾の「報告」に挙がっている項目を出力してください。
```

## 2. A-2 ＋ C 経営レイヤー定例：次回分の準備と取締役提案の転記（金12:00）

```
会議議事録の自動化「A-2 ＋ C（経営レイヤー定例：次回分の準備と取締役提案の転記）」を実行してください。

リポジトリ meeting-automation がこのセッションにチェックアウトされています。

【手順】
1. prompts/_shared-rules.md を読む。全処理に共通するルールで、個別プロンプトの手順より優先する。
2. config/meetings.json を読む。
3. prompts/exec-layer-prep.md を読み、書かれているとおりに実行する。手順4が処理C（取締役週次定例からの転記）。

【対象日】
TARGET_DATE = 実行日（JST）。「次回」は、この日から10日先までのカレンダー予定のうち、いちばん近い該当予定を指す。

【厳守】
- Slackは config の slack_mode に従う。
- カレンダーに次回予定がない場合は、何も書かずに報告して終了する。
- 書き込んでよいのは3箇所だけ。新規ページのプロパティ、0. の「🟦 前回の持ち帰り（自動転記）」の表、アジェンダ4の「🟩 事前記入」の表。それ以外の 🟩 / 🟦 / 🤖 / 🟨 には触れない。
- 処理Cの読み取り元は、取締役週次定例の 🟦「執行役員連携事項」の表だけ。「取締役課題」の表は読まない。🟦 トグルがないページからは転記せず、候補として報告するだけにする。

最後に、prompts/exec-layer-prep.md 末尾の「報告」に挙がっている項目を出力してください。
```

## 3. B-1 取締役週次定例：議事録生成（月12:00）

```
会議議事録の自動化「B-1（取締役週次定例：会議後の議事録生成とSlack投稿）」を実行してください。

リポジトリ meeting-automation がこのセッションにチェックアウトされています。

【手順】
1. prompts/_shared-rules.md を読む。全処理に共通するルールで、個別プロンプトの手順より優先する。
2. config/meetings.json を読む。
3. prompts/board-weekly-minutes.md を読み、書かれているとおりに実行する。

【対象日】
TARGET_DATE = 実行日（JST）。プロンプト内の「本日」「当日」はすべてこの日を指す。

【厳守】
- Slackは config の slack_mode に従う。
- 文字起こしがまだ生成されていない場合は、何も書かずに報告して終了する。翌日のリトライで拾う。
- ステータスが「自動生成済」以降なら二重実行を避けて終了する。
- 書き込むのは 🤖 と ⏺️ のトグル、およびプロパティだけ。🟩 と 🟦 には触れない。書き込み後に再取得して 🟩 / 🟦 が変わっていないことを必ず確認する。差分があればSlackへ投稿せず報告する。

最後に、prompts/board-weekly-minutes.md 末尾の「報告」に挙がっている項目を出力してください。
```

## 4. B-2 経営レイヤー定例：議事録生成（月17:00）

```
会議議事録の自動化「B-2（経営レイヤー定例：会議後の議事録生成とSlack投稿）」を実行してください。

リポジトリ meeting-automation がこのセッションにチェックアウトされています。

【手順】
1. prompts/_shared-rules.md を読む。全処理に共通するルールで、個別プロンプトの手順より優先する。
2. config/meetings.json を読む。
3. prompts/exec-layer-minutes.md を読み、書かれているとおりに実行する。

【対象日】
TARGET_DATE = 実行日（JST）。プロンプト内の「本日」「当日」はすべてこの日を指す。

【厳守】
- Slackは config の slack_mode に従う。
- 文字起こしがまだ生成されていない場合は、何も書かずに報告して終了する。翌日のリトライで拾う。
- ステータスが「記録済」または「確認済」なら二重実行を避けて終了する。このDBに「自動生成済」は存在しない。
- 埋めるのはアジェンダ1〜4の「🤖 議論の記録」とプロパティだけ。🟩 / 🟦 5.今日決まったこと / 🟨 6. には触れない。書き込み後に再取得して確認し、差分があればSlackへ投稿せず報告する。

最後に、prompts/exec-layer-minutes.md 末尾の「報告」に挙がっている項目を出力してください。
```

## 5. リトライ：両会議の議事録生成（火08:00）

```
会議議事録の自動化のリトライを実行してください。前日（月曜）の2会議が、Meetの文字起こしの生成待ちで空振りしていた場合に拾うためのものです。

リポジトリ meeting-automation がこのセッションにチェックアウトされています。

【対象日】
TARGET_DATE = 実行日の前日（JST）。プロンプト内の「本日」「当日」はすべてこの日を指す。実行日ではないことに注意。

【手順】
1. prompts/_shared-rules.md と config/meetings.json を読む。
2. prompts/board-weekly-minutes.md（B-1）を、上記のTARGET_DATEで実行する。
3. 続けて prompts/exec-layer-minutes.md（B-2）を、同じTARGET_DATEで実行する。
4. 2つの結果をまとめて報告する。

【厳守】
- ステータスが既に進んでいるページ（取締役週次定例なら「自動生成済」以降、経営レイヤー定例なら「記録済」以降）は、二重実行を避けて終了する。**これが通常の結果。** 月曜の1本目が成功していれば、両方ともここで終わる。
- 前日に該当する会議がなければ、その旨だけ報告して終了する。
- Slackは config の slack_mode に従う。
- 2の結果にかかわらず3も実行する。片方が終了条件に当たっても、もう片方は動かす。
```

---

## 登録後の確認

1. 各ルーティンを一度手で発火させ、コネクタが効いているか（Notion / Slack / Calendar / Drive のツールが呼べているか）を見る
2. B-1 / B-2 は対象日にページがないと終了するので、初回の手動発火は「予定なし」で終わるのが正常
3. 初回の本番は 2026-08-28（金）12:00 の A-1 / A-2
