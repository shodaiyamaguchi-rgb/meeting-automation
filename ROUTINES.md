# ルーティン登録手順（claude.ai の Routines UI から）

`crontab` はサーバにcronを置く場合の定義。常時起動のマシンを用意しないなら、**claude.ai のルーティンとして登録する**のが現実的。

## 登録先

**https://claude.ai/code/routines** → **New routine**

Claude Code on the web のセッション内からは**完結しない。** `/schedule` はwebセッションでは使えず、MCPツール（`create_trigger`）で作ったルーティンは**リポジトリとコネクタが空のまま**になる。ローカルのターミナルからなら `/schedule` でも作れるが、UIのほうが項目が見えるので確実。

### MCPツールで作った場合に、UIで足す必要があるもの

`create_trigger` は名前・スケジュール・プロンプトだけを持ったルーティンを作る。次の2つが空なので、**UIで開いて足さないと動かない。**

| 項目 | 症状 |
|---|---|
| リポジトリ（`sources`） | 空。プロンプトが `prompts/*.md` を読もうとしても、リポジトリが無いので読めずに終わる |
| コネクタ（`mcp_connections`） | 空。Notion / Slack / Calendar / Drive のツールが呼べず、何もできずに終わる |

`connectors` パラメータは組織によっては使えない（このワークスペースでは拒否される）。**足すのはUIから。** 一度足せば以降の発火にも引き継がれる。

2026-09-06 に D-1 / D-2 をこの方法で作成した。

| 処理 | trigger ID |
|---|---|
| D-1 | `trig_015ygbuXj6Gc1V4Wdej4QcGt` |
| ~~D-2~~ | ~~`trig_014uQjbjmBUTuPYTMGeaF3LL`~~（2026-09-08廃止。D-1に統合したため、**UIから手動で削除すること。** このセッションのツールからは削除できない） |

**2026-09-08、D-1 / D-2 の2本構成を D-1 単体に統合した。** Slack DM を廃止し、承認はD-1のセッション内でのオーナーの返信で確認する。D-1 のルーティンに登録済みのプロンプト（下記6番）も、UI上で新しい文面に**差し替えが必要。** リポジトリの `ROUTINES.md` を更新しても、登録済みルーティンのプロンプトは自動的には追従しない。

## 共通設定（7本とも同じ）

作成画面の項目は上から順に、**名前 → プロンプト → リポジトリ → 環境 → トリガー → コネクタ** の6つ。

| 項目 | 設定する値 |
|---|---|
| 名前 | 下の表のとおり |
| プロンプト | 各節のコードブロックをそのまま貼る |
| リポジトリ | `shodaiyamaguchi-rgb/meeting-automation` |
| 環境 | Default で足りる。コネクタの通信はAnthropic経由なので、許可ドメインの追加は不要 |
| トリガー | **Schedule** →「毎週（weekly）」で曜日と時刻を指定 |
| コネクタ | Notion / Slack / Google Calendar / Google Drive の4つを残し、**他は外す** |

**時刻はローカル時間（JST）でそのまま入れる。** UTCへの換算は不要で、システムが自動変換する。

コネクタは既定で「接続済みのもの全部」が入っている。ルーティンの実行中は権限確認なしに全ツールを使えるので、不要なものは外しておく。

**リポジトリはデフォルトブランチから複製される。** このリポジトリのデフォルトブランチは `main`。実装はすべてそこにマージ済みなので、**登録前にやることはない。**

ブランチで実装を足したときは、**`main` へマージしてからルーティンを登録・実行すること。** マージ前に実行すると、ルーティンは古い `main` を複製し、足したプロンプトが存在しないまま動く。

実行の数分の遅れ（stagger）が入る。12:00指定でも12:03などに始まることがある。

## 登録する6本

| # | 名前 | スケジュール（JST） |
|---|---|---|
| 1 | A-1 取締役週次定例：次回分の準備 | 毎週 金 12:00 |
| 2 | A-2 ＋ C 経営レイヤー定例：次回分の準備 | 毎週 金 12:00 |
| 3 | B-1 取締役週次定例：議事録生成 | 毎週 月 12:00 |
| 4 | B-2 経営レイヤー定例：議事録生成 | 毎週 月 17:00 |
| 5 | リトライ（両会議まとめて） | 毎週 火 12:00 |
| 6 | D-1 取締役週次定例：執行役員向け共有案の作成・確認・投稿 | 毎週 月 13:00 |

5番のリトライは、Meetの文字起こしの生成が遅れて月曜の2本が空振りした場合に前日分を拾うためのもの。1本目が成功していればステータスが進んでいるので、二重実行を避けて即終了する。

**6番は単体で1つの処理（2026-09-08にD-1／D-2の2本構成から統合）。** 案を作ってこのセッション上でオーナーに提示し、オーナーがセッション内で承認を返したら、その場でチャンネルへ本体とスレッドを投稿する。承認が返らないかぎり投稿しない。D-1 にリトライ枠は置かない（対象日の当日を過ぎてから承認が返っても投稿しないため、翌日に走らせても必ず空振りする）。

月曜の並びは 10:00 取締役週次定例 → 12:00 B-1 → **13:00 D-1（案を提示。以降はオーナーの承認待ち）** → 15:00 経営レイヤー定例 → 17:00 B-2 → **18:00めどに投稿（承認され次第、同じセッション内で）**。投稿は本体とスレッドの2本で、スレッドに当日の議論の内容を続けて出す。

---

## 1. A-1 取締役週次定例：次回分の準備（金12:00）

```
会議議事録の自動化「A-1（取締役週次定例：次回分の準備）」を実行してください。

リポジトリ meeting-automation がこのセッションにチェックアウトされています。

【手順】
1. prompts/_shared-rules.md を読む。全処理に共通するルールで、個別プロンプトの手順より優先する。
2. config/meetings.json を読む。Notion ID・Slackチャンネル・カレンダー予定名・slack_mode が入っている。
3. prompts/board-weekly-prep.md を読み、書かれているとおりに実行する。

【対象日】
TARGET_DATE = 実行日（JST）。「次回」は、この日から10日先までのカレンダー予定のうち、いちばん近い該当予定を指す。

【厳守】
- Slackは config の slack_mode に従う。
- カレンダーに次回予定がない場合は、Notionに部分的に書き込まず、何も書かずに報告して終了する。
- 書き込んでよいのは、新規作成したページのプロパティと、🟩 トグル内「前回の持ち帰り（自動転記）」の表だけ。それ以外の 🟩 / 🟦 / 🤖 / ⏺️ には触れない。
- 同じ日付のページが既にある場合は作成せず、Slack投稿だけ行う。

最後に、prompts/board-weekly-prep.md 末尾の「報告」に挙がっている項目を出力してください。
```

## 2. A-2 ＋ C 経営レイヤー定例：次回分の準備と取締役提案の転記（金12:00）

```
会議議事録の自動化「A-2 ＋ C（経営レイヤー定例：次回分の準備と取締役提案の転記）」を実行してください。

リポジトリ meeting-automation がこのセッションにチェックアウトされています。

【手順】
1. prompts/_shared-rules.md を読む。全処理に共通するルールで、個別プロンプトの手順より優先する。
2. config/meetings.json を読む。
3. prompts/exec-layer-prep.md を読み、書かれているとおりに実行する。手順4が処理C（取締役週次定例からの転記）。

【対象日】
TARGET_DATE = 実行日（JST）。「次回」は、この日から10日先までのカレンダー予定のうち、いちばん近い該当予定を指す。

【厳守】
- Slackは config の slack_mode に従う。
- カレンダーに次回予定がない場合は、何も書かずに報告して終了する。
- 書き込んでよいのは3箇所だけ。新規ページのプロパティ、0. の「🟦 前回の持ち帰り（自動転記）」の表、アジェンダ4の「🟩 事前記入」の表。それ以外の 🟩 / 🟦 / 🤖 / 🟨 には触れない。
- 処理Cの読み取り元は、取締役週次定例の 🟦「執行役員連携事項」の表だけ。「取締役課題」の表は読まない。🟦 トグルがないページからは転記せず、候補として報告するだけにする。

最後に、prompts/exec-layer-prep.md 末尾の「報告」に挙がっている項目を出力してください。
```

## 3. B-1 取締役週次定例：議事録生成（月12:00）

```
会議議事録の自動化「B-1（取締役週次定例：会議後の議事録生成とSlack投稿）」を実行してください。

リポジトリ meeting-automation がこのセッションにチェックアウトされています。

【手順】
1. prompts/_shared-rules.md を読む。全処理に共通するルールで、個別プロンプトの手順より優先する。
2. config/meetings.json を読む。
3. prompts/board-weekly-minutes.md を読み、書かれているとおりに実行する。

【対象日】
TARGET_DATE = 実行日（JST）。プロンプト内の「本日」「当日」はすべてこの日を指す。

【厳守】
- Slackは config の slack_mode に従う。
- 文字起こしがまだ生成されていない場合は、何も書かずに報告して終了する。翌日のリトライで拾う。
- ステータスが「自動生成済」以降なら二重実行を避けて終了する。
- 書き込むのは 🤖 と ⏺️ のトグル、およびプロパティだけ。🟩 と 🟦 には触れない。書き込み後に再取得して 🟩 / 🟦 が変わっていないことを必ず確認する。差分があればSlackへ投稿せず報告する。

最後に、prompts/board-weekly-minutes.md 末尾の「報告」に挙がっている項目を出力してください。
```

## 4. B-2 経営レイヤー定例：議事録生成（月17:00）

```
会議議事録の自動化「B-2（経営レイヤー定例：会議後の議事録生成とSlack投稿）」を実行してください。

リポジトリ meeting-automation がこのセッションにチェックアウトされています。

【手順】
1. prompts/_shared-rules.md を読む。全処理に共通するルールで、個別プロンプトの手順より優先する。
2. config/meetings.json を読む。
3. prompts/exec-layer-minutes.md を読み、書かれているとおりに実行する。

【対象日】
TARGET_DATE = 実行日（JST）。プロンプト内の「本日」「当日」はすべてこの日を指す。

【厳守】
- Slackは config の slack_mode に従う。
- 文字起こしがまだ生成されていない場合は、何も書かずに報告して終了する。翌日のリトライで拾う。
- ステータスが「記録済」または「確認済」なら二重実行を避けて終了する。このDBに「自動生成済」は存在しない。
- 埋めるのはアジェンダ1〜4の「🤖 議論の記録」とプロパティだけ。🟩 / 🟦 5.今日決まったこと / 🟨 6. には触れない。書き込み後に再取得して確認し、差分があればSlackへ投稿せず報告する。

最後に、prompts/exec-layer-minutes.md 末尾の「報告」に挙がっている項目を出力してください。
```

## 5. リトライ：両会議の議事録生成（火12:00）

```
会議議事録の自動化のリトライを実行してください。前日（月曜）の2会議が、Meetの文字起こしの生成待ちで空振りしていた場合に拾うためのものです。

リポジトリ meeting-automation がこのセッションにチェックアウトされています。

【対象日】
TARGET_DATE = 実行日の前日（JST）。プロンプト内の「本日」「当日」はすべてこの日を指す。実行日ではないことに注意。

【手順】
1. prompts/_shared-rules.md と config/meetings.json を読む。
2. prompts/board-weekly-minutes.md（B-1）を、上記のTARGET_DATEで実行する。
3. 続けて prompts/exec-layer-minutes.md（B-2）を、同じTARGET_DATEで実行する。
4. 2つの結果をまとめて報告する。

【厳守】
- ステータスが既に進んでいるページ（取締役週次定例なら「自動生成済」以降、経営レイヤー定例なら「記録済」以降）は、二重実行を避けて終了する。**これが通常の結果。** 月曜の1本目が成功していれば、両方ともここで終わる。
- 前日に該当する会議がなければ、その旨だけ報告して終了する。
- Slackは config の slack_mode に従う。
- 2の結果にかかわらず3も実行する。片方が終了条件に当たっても、もう片方は動かす。
```

## 6. D-1 取締役週次定例：執行役員向け共有案の作成・確認・投稿（月13:00）

```
会議議事録の自動化「D-1（取締役週次定例：執行役員レイヤーへの共有案の作成・確認・投稿）」を実行してください。

リポジトリ meeting-automation がこのセッションにチェックアウトされています。

【手順】
1. prompts/_shared-rules.md を読む。全処理に共通するルールで、個別プロンプトの手順より優先する。
2. config/meetings.json を読む。meetings.board-weekly.exec_digest に投稿先・時刻が入っている。
3. prompts/board-weekly-exec-digest.md を読み、書かれているとおりに実行する。

【対象日】
TARGET_DATE = 実行日（JST）。「本日」「当日」はすべてこの日を指す。

【厳守】
- Notionには一切書き込まない。読むだけ。
- 承認が確認できないかぎり、Slackのチャンネルへ投稿しない。Slack DMは使わない。承認は、このセッション内でのオーナーの返信だけで判定する。
- 案は本体（サマリー）とスレッド（当日の議論の内容）の2本を作る。サマリーだけを出さない。
- 材料にしてよい欄は prompts/board-weekly-exec-digest.md 手順2の表に従う。本体とスレッドで範囲が違う。🟦「取締役課題」／🟩 の2欄／🤖 5.・6.／⏺️ は、どちらの材料にもしない。
- スレッド本文の冒頭に、どの範囲を外したのかを必ず書く。黙って落とさない。
- 「執行役員のみなさんへ」の欄は 🟦「執行役員連携事項」の表からだけ作る。表が空なら見出しごと省略する。🤖 5. の候補や「取締役課題」で補わない。
- 役員でない個人の名前を出さない。名前を伏せた形でも出さない。
- Notionページ・Google Docs・スライドへのリンクを投稿案に貼らない。
- ステータスが「事前記入中」（＝ B-1 未実行で 🤖 が空）のときは、案を作らずに報告して終了する。
- 載せるか迷ったものは載せず、オーナーへ提示する内容の「載せなかった件」に理由つきで書く。
- 承認が返ってから投稿するまでの本文は、直前に提示した案から一字一句そのまま使う。この処理で文面を新たに作らない・付け足さない（オーナーが個別に指示した修正だけは反映する）。
- 対象日の当日を過ぎてから承認が返った場合は投稿せず、報告してオーナーに確認する。

最後に、prompts/board-weekly-exec-digest.md 末尾の「報告」に挙がっている項目を出力してください。
```

---

## 登録後の確認

1. 各ルーティンを一度手で発火させ、コネクタが効いているか（Notion / Slack / Calendar / Drive のツールが呼べているか）を見る
2. B-1 / B-2 は対象日にページがないと終了するので、初回の手動発火は「予定なし」で終わるのが正常
3. 初回の本番は 2026-08-28（金）12:00 の A-1 / A-2
4. D-1 は、セッションの出力として案が提示され、「載せなかった件」が理由つきで並んでいるかを見る。ここが空のまま出てきたら、精査が効いていない
5. D-1 に承認を返さずに一度走らせ、**投稿されないこと**を確認してから運用に入る
6. 初回の本投稿では、本体とスレッドの2本が並んでいるか、スレッドが `reply_broadcast` でチャンネルに二重に流れていないかを見る
